# 1차 산업 — 벌목 (Logging)

> 상위: [`자원채취`](../README.md) · [`게임기획코어.md`](../../게임기획코어.md)
> 최종 업데이트: 2026-08-02 · 상태: **정체성 제안 · 수치 미정**
> **바뀌면 갱신:** [`게임기획코어`](../../게임기획코어.md) · [`기획평가`](../../기획평가.md) · [`자원채취`](../README.md)
> enum: `ItemType.Logging = 4` ✅ 존재

---

## 1. 산업 정체성 (제안)

> ⚠️ 제안이다.

| 항목 | 값 |
| --- | --- |
| **심리 정의** | **절제하면 지속되는 수익** |
| 투자 비유 | 배당주 — 원금을 건드리지 않아야 계속 나온다 |
| 한 줄 | **베면 줄고, 시간이 지나면 돌아온다** |
| 회당 산출 | 중간 — **숲 자원량에 비례해 변동** |
| 실패율 | 없음 |
| ~~특별보상(10%)~~ | ❌ **층 폐지 (2026-07-30).** 고급 원목의 경로 미정 |
| 역할 | 경제의 **속도 조절** — 무한 채취 방지 |
| 플레이어 개입 | 벌목 강도 조절 (보존 ↔ 남벌) |

> 벌목의 차별점은 **회당 산출이 자원량에 따라 움직인다**는 것 —
> 5종 중 유일하게 산출이 변동하는 산업이다.
> 희귀도 분포도 벌목만 다르게 둘 수 있지만(2026-07-31), **고유 메커니즘이 이미 강해서
> 굳이 기울일 필요가 없다.** 기준값으로 두는 편이 낫다.

> 주기 통일(30초)의 영향이 **가장 적은 산업**이다.
> 벌목의 고유 축은 주기가 아니라 **자원량 상태**이므로 그대로 유지된다.
> 다만 30초마다 자원이 줄어들므로 **고갈 속도와 재생률을 그 빈도에 맞춰** 잡아야 한다.

벌목은 5개 산업 중 **유일하게 자원 총량에 상한이 있는** 구조다.
"많이 캘수록 다음에 덜 나온다"는 감가 메커니즘을 담당한다.

---

## 2. 채취 특성 (제안)

```
숲 자원량 ─벌목→ 감소 ─시간→ 재생 ─→ 회복
```

| 파라미터 | 값 | 비고 |
| --- | --- | --- |
| 채취주기 | **30초** ✅ | 전 산업 공통 → [자원채취](../README.md) 2.1 |
| 회당 산출 | 중간 ⚠️ | **숲 자원량에 비례**. 수치 미정 |
| 실패율 | 없음 ✅ | |
| 희귀도 분포 | **벌목 고유** ✅ | 2026-07-31. `LoggingBasicTable.Weight`가 벌목의 분포다 |
| ~~특별보상(10%)~~ | ❌ 폐지 | 2026-07-30. 고급 원목은 희귀도 롤 편입 검토 |
| 특이 규칙 | 자원 고갈 | 남벌 시 산출량 하락 |

> ⚠️ **고갈 속도는 30초 주기 기준으로 계산한다.** 하루 2,880회 벌목이 일어나므로,
> 판정당 감소량을 크게 잡으면 숲이 몇 시간 만에 바닥난다.
> 재생률도 같은 시간축에서 잡아야 "자고 일어나면 회복"이 성립한다.

### 자원 고갈·재생 (제안)

- 숲마다 `자원량(0~100%)`이 있고, 벌목하면 감소한다.
- 시간이 지나면 자연 회복한다.
- 자원량이 낮으면 회당 산출이 줄어든다.

| 논점 | 권장 |
| --- | --- |
| 완전 고갈 가능? | **불가** — 하한(예: 20%)을 두어 채취가 멈추지 않게 |
| 재생 속도 | 하루 방치로 완전 회복되는 수준 |
| ~~오프라인 중 재생~~ | ⚠️ **질문이 바뀌었다 (2026-07-29).** 오프라인 진행이 폐지돼 "오프라인 중"이라는 구간 자체가 없다 |

> ⚠️ **비접속 중 자원이 재생되는가는 다시 정해야 한다.**
> 채취는 접속 중에만 돌지만, **재생까지 멈추면** 오래 안 들어온 유저의 숲이
> 고갈된 채로 굳는다(접속하자마자 산출이 바닥). 반대로 **재생만 오프라인에 두면**
> "안 들어오는 것이 이득"이 되어 P2와 부딪힌다.
> 재생을 **접속 시각 기준으로 소급 계산**하는 방식이 현재로선 가장 무난하다 — 미결.

> **주의:** 고갈이 "접속해서 관리해야 함"으로 이어지면 P1을 위반한다.
> 고갈은 **총량 상한**을 위한 장치이지, 플레이어에게 노동을 시키기 위한 장치가 아니다.
> 하한을 두고, 방치 시 자동으로 지속 가능한 속도에 수렴하게 만드는 편이 안전하다.

---

## 3. 산출물

| 구분 | 내용 |
| --- | --- |
| 기본 자원 | 목재 (수종별 등급) |
| 희귀 산출 | 고급 원목 |
| 용도 | ~~특성 재료~~(폐지) · **즉시 판매** · 거래소 · 캐릭터 먹이기 · 제작 재료(도입 시) |

`ItemTable.ItemType = Logging`. ~~현재 더미 데이터(`D`/`E`)~~ —
**실 데이터 30종이 들어갔고**(2026-07-31: 잡목 · 소나무 목재 · 참나무 목재 · 흑단 목재 ·
고급 원목 · 세계수 가지 + Lv2~5 각 6종) TID 대역은 `30001~30046`이다.
→ [아이템](../../아이템/README.md) 1.3

> ⚠️ **"특성 재료"는 폐지됐다** (2026-08-01). 특성은 계정 레벨 포인트로 찍는다.

---

## 4. 데이터 설계 (제안)

| 테이블 | 컬럼 |
| --- | --- |
| ~~`TreeTable`~~ | ❌ **폐지 (2026-08-01).** 수종 = 벌목 레벨. `IndustryLevelTable`이 대신한다 |
| `LoggingBasicTable` | `DropTID`(키), **`IndustryLevel`**(수종), `ItemTID`, `Weight` |
| ~~`LoggingSpecialTable`~~ | ❌ **폐지 (2026-07-30).** 특별보상 층이 없어졌다 |

숲 자원량은 정적 테이블이 아니라 **유저 DB**에 저장한다 (유저별 상태).

### 수종 = 벌목 레벨 (2026-08-01)

`TreeTable`이 하려던 "수종별로 산출물이 갈린다"는 [산업 레벨](../산업레벨.md)이 담당한다.
**자원 고갈·재생은 레벨과 직교한다** — 레벨이 무엇이든 숲 자원량은 별개로 움직인다.

| 축 | 어디에 |
| --- | --- |
| 수종(무엇이 나오는가) | **슬롯의 `IndustryLevel`** |
| 자원량(얼마나 나오는가) | **유저 DB** — 벌목의 고유 메커니즘 |

> ⚠️ **자원량을 레벨별로 따로 둘지 정해야 한다.** 하나로 두면 레벨을 바꿔도
> 고갈이 이어지고, 레벨별로 두면 **고갈될 때마다 레벨을 갈아타는 회피**가 가능해진다.
> 벌목의 "절제하면 지속된다"는 정체성이 여기 걸려 있다.

> `DepletionPerCut`·`RegenPerHour`는 `IndustryLevelTable`에 컬럼으로 붙이거나
> 벌목 전용 시트로 남긴다. → [산업 레벨](../산업레벨.md) 5장

> `DepletionPerCut`은 **30초 판정 1회당 감소량**이다. 하루 2,880회가 곱해진다.

---

## 5. 결정 필요

1. 자원 고갈 메커니즘을 실제로 넣는가? (넣지 않으면 농사와 차별점이 사라진다)
2. **판정당 고갈량과 시간당 재생률은?** (30초 × 2,880회/일 기준으로 계산할 것)
3. 자원량 하한은?
4. ~~수종이 여러 개인가? 선택 가능한가?~~ ✅ **해소 (2026-08-01)** — 수종 = 벌목 레벨.
   여러 개이고 배치할 때 고른다 → [산업 레벨](../산업레벨.md)
4-1. **숲 자원량을 레벨별로 따로 두는가, 하나로 두는가?** (4장 — 고갈 회피와 직결)
5. 도끼 같은 도구 개념이 있는가?

> 해소됨: 채취주기 30초 ✅ / 실패 없음 ✅
